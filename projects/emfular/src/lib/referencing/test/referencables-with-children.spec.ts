import { ReferencablesWithChildren } from './referencables-with-children';

describe('ReferencablesWithChildren', () => {
  it('should create an instance', () => {
    expect(new ReferencablesWithChildren()).toBeTruthy();
  });
});
