import { ReferencableListContainer } from './referencable-list-container';

describe('ReferencableListContainer', () => {
  it('should create an instance', () => {
    expect(new ReferencableListContainer()).toBeTruthy();
  });
});
