import { ReferencableTreeParentContainer } from './referencable-tree-parent-container';

describe('ReferencableTreeParentContainer', () => {
  it('should create an instance', () => {
    expect(new ReferencableTreeParentContainer()).toBeTruthy();
  });
});
